module.exports = (ctx) => ({
  plugins: {
    autoprefixer: {
      /**
       * @module autoprefixer
       * @url https://www.npmjs.com/package/autoprefixer
       * --------------------------------------------------------- */
      grid: 'autoplace',
    },
  },
});
